setupRequired("python")
setupRequired("scipy")
setupRequired("numpy")
envAppend(PYTHONPATH, ${PRODUCT_DIR}/python)
envAppend(PATH, ${PRODUCT_DIR}/bin)
envSet(DUST_DIR,${PRODUCT_DIR}/etc)
envSet(XCORRECT_PATH,${PRODUCT_DIR}/etc)
